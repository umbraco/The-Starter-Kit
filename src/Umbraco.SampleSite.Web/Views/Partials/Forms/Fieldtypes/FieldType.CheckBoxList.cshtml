@model Umbraco.Forms.Mvc.Models.FieldViewModel
@{ var i = 0; }
          
<span class="checkboxlist" id="@Model.Id"  data-umb="@Model.Id">
@foreach (Umbraco.Forms.Mvc.Models.PrevalueViewModel pv in Model.PreValues)  
{
    <input type="checkbox" name="@Model.Name" id="@string.Concat(Model.Id,"_",i)" data-umb="@string.Concat(Model.Id,"_",i)"  value="@pv.Value" 
    @if (Model.Mandatory) {<text>data-val="true" data-val-required="@Model.RequiredErrorMessage"</text>}
    @if (Model.ContainsValue(pv.Value)){<text>checked="checked"</text>}/>
    
    <label for="@string.Concat(Model.Id,"_",i)">@pv.Value</label>
    <br />

    i++;
}
</span>